# Flutter foundation

This is a minimal Week 1 Flutter application shell. It deliberately does not contain authentication, data persistence, observation entry, NLP calls, reports, or dashboards.

With Flutter installed, run `flutter pub get`, `flutter analyze`, and `flutter run` in this directory. If a local platform runner is needed, use `flutter create .` to generate the standard Android/iOS/web/desktop runner files.
